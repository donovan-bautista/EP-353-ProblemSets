#include <stdio.h>
// this code's purpose is to showcase the variable function using MIDI pitch values by printing them out.
int main(){
    int C4 = 60, A4 = 69; // assigns values to two integers 
        printf("The MIDI pitch for C4 is %i.\n", C4); // prints two messages, assigning each one its respective value 
        printf("The MIDI pitch for A4 is %i.\n", A4);
}